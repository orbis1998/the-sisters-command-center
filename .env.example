# Supabase (required — exposed to the browser via Vite)
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_anon_public_key

# Optional — local migration scripts only (not needed on Vercel)
DATABASE_URL=
